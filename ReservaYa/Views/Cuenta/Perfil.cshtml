@model ReservaYa.Models.Usuarios

@{
    ViewBag.Title = "Mi Perfil";
}

<div class="container mt-5">

    <h2 class="text-center mb-4">Mi Perfil</h2>

    <div class="card shadow-sm p-4" style="max-width: 600px; margin: auto;">

        <div class="text-center mb-4">
            @{
                // Usa la nueva propiedad RutaImagen. Si está vacía, usa la imagen por defecto.
                string rutaImagen = !string.IsNullOrEmpty(Model.RutaImagen)
                    ? Model.RutaImagen
                    : "/Content/Uploads/perfiles/default-profile.png"; 
            }
            <img src="@rutaImagen"
                 alt="Foto de Perfil"
                 class="rounded-circle"
                 style="width: 150px; height: 150px; object-fit: cover; border: 3px solid #007bff;" />

            <br />
            <a href="@Url.Action("CambiarImagen", "Cuenta")" class="btn btn-sm btn-outline-secondary mt-2">
                Cambiar Imagen
            </a>
        </div>
        <h4 class="mb-3">Información Personal</h4>

        <p><strong>Nombre:</strong> @Model.Nombres</p>
        <p><strong>Correo:</strong> @System.Text.Encoding.UTF8.GetString(Model.Correo)</p>
        <p><strong>Apellidos:</strong> @Model.Apellidos</p>
        <p><strong>Fecha de Nacimiento:</strong> @Model.FechaNacimiento</p>

        <hr />

        <div class="text-center">
            <a href="@Url.Action("CambiarCredenciales", "Cuenta")"
               class="btn btn-primary btn-lg">
                Cambiar Correo / Contraseña
            </a>
        </div>

    </div>

</div>